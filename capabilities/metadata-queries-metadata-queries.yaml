naftiko: 1.0.0-alpha2
info:
  label: Box Metadata Queries API — Metadata Queries
  description: 'Box Metadata Queries API — Metadata Queries. 1 operations. Lead operation: Box Query files/folders by metadata.
    Self-contained Naftiko capability covering one Box business surface.'
  tags:
  - Box
  - Metadata Queries
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BOX_API_KEY: BOX_API_KEY
capability:
  consumes:
  - type: http
    namespace: metadata-queries-metadata-queries
    baseUri: ''
    description: Box Metadata Queries API — Metadata Queries business capability. Self-contained, no shared references.
    resources:
    - name: metadata_queries-execute_read
      path: /metadata_queries/execute_read
      operations:
      - name: postmetadataqueriesexecuteread
        method: POST
        description: Box Query files/folders by metadata
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: metadata-queries-metadata-queries-rest
    port: 8080
    description: REST adapter for Box Metadata Queries API — Metadata Queries. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/metadata-queries/execute-read
      name: metadata-queries-execute-read
      description: REST surface for metadata_queries-execute_read.
      operations:
      - method: POST
        name: postmetadataqueriesexecuteread
        description: Box Query files/folders by metadata
        call: metadata-queries-metadata-queries.postmetadataqueriesexecuteread
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: metadata-queries-metadata-queries-mcp
    port: 9090
    transport: http
    description: MCP adapter for Box Metadata Queries API — Metadata Queries. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: box-query-files-folders-metadata
      description: Box Query files/folders by metadata
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: metadata-queries-metadata-queries.postmetadataqueriesexecuteread
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
