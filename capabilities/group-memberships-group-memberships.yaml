naftiko: 1.0.0-alpha2
info:
  label: Box Group Memberships API — Group Memberships
  description: 'Box Group Memberships API — Group Memberships. 4 operations. Lead operation: Box Add user to group. Self-contained
    Naftiko capability covering one Box business surface.'
  tags:
  - Box
  - Group Memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    BOX_API_KEY: BOX_API_KEY
capability:
  consumes:
  - type: http
    namespace: group-memberships-group-memberships
    baseUri: ''
    description: Box Group Memberships API — Group Memberships business capability. Self-contained, no shared references.
    resources:
    - name: group_memberships
      path: /group_memberships
      operations:
      - name: postgroupmemberships
        method: POST
        description: Box Add user to group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: A comma-separated list of attributes to include in the
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: group_memberships-group_membership_id
      path: /group_memberships/{group_membership_id}
      operations:
      - name: getgroupmembershipsid
        method: GET
        description: Box Get group membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_membership_id
          in: path
          type: string
          description: The ID of the group membership.
          required: true
        - name: fields
          in: query
          type: array
          description: A comma-separated list of attributes to include in the
      - name: putgroupmembershipsid
        method: PUT
        description: Box Update group membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_membership_id
          in: path
          type: string
          description: The ID of the group membership.
          required: true
        - name: fields
          in: query
          type: array
          description: A comma-separated list of attributes to include in the
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: deletegroupmembershipsid
        method: DELETE
        description: Box Remove user from group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_membership_id
          in: path
          type: string
          description: The ID of the group membership.
          required: true
  exposes:
  - type: rest
    namespace: group-memberships-group-memberships-rest
    port: 8080
    description: REST adapter for Box Group Memberships API — Group Memberships. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/group-memberships
      name: group-memberships
      description: REST surface for group_memberships.
      operations:
      - method: POST
        name: postgroupmemberships
        description: Box Add user to group
        call: group-memberships-group-memberships.postgroupmemberships
        with:
          fields: rest.fields
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/group-memberships/{group-membership-id}
      name: group-memberships-group-membership-id
      description: REST surface for group_memberships-group_membership_id.
      operations:
      - method: GET
        name: getgroupmembershipsid
        description: Box Get group membership
        call: group-memberships-group-memberships.getgroupmembershipsid
        with:
          group_membership_id: rest.group_membership_id
          fields: rest.fields
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: putgroupmembershipsid
        description: Box Update group membership
        call: group-memberships-group-memberships.putgroupmembershipsid
        with:
          group_membership_id: rest.group_membership_id
          fields: rest.fields
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletegroupmembershipsid
        description: Box Remove user from group
        call: group-memberships-group-memberships.deletegroupmembershipsid
        with:
          group_membership_id: rest.group_membership_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: group-memberships-group-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Box Group Memberships API — Group Memberships. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: box-add-user-group
      description: Box Add user to group
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: group-memberships-group-memberships.postgroupmemberships
      with:
        fields: tools.fields
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: box-get-group-membership
      description: Box Get group membership
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: group-memberships-group-memberships.getgroupmembershipsid
      with:
        group_membership_id: tools.group_membership_id
        fields: tools.fields
      outputParameters:
      - type: object
        mapping: $.
    - name: box-update-group-membership
      description: Box Update group membership
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: group-memberships-group-memberships.putgroupmembershipsid
      with:
        group_membership_id: tools.group_membership_id
        fields: tools.fields
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: box-remove-user-group
      description: Box Remove user from group
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: group-memberships-group-memberships.deletegroupmembershipsid
      with:
        group_membership_id: tools.group_membership_id
      outputParameters:
      - type: object
        mapping: $.
